import { ExperienciaComponent } from '../components/experiencia/experiencia.component';

describe('ExperienciaSkipTests', () => {
  it('should create an instance', () => {
    expect(new ExperienciaComponent()).toBeTruthy();
  });
});
